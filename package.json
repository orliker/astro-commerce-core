{
  "name": "astro-commerce-core-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Auditable commerce domain engines: order state transitions, margin-aware pricing, runtime gates and SQLite tests.",
  "workspaces": ["packages/*"],
  "engines": { "node": ">=24.0.0" },
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "devDependencies": { "@types/node": "24.0.0", "typescript": "5.9.3", "vitest": "3.2.4" }
}
